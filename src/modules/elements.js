const todoList = document.getElementById('tasklist');
const todoInput = document.getElementById('new-task');
const clear = document.getElementById('clear-btn');

export { todoList, todoInput, clear };